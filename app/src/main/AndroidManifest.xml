<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Otusfragmentlesson"
        tools:targetApi="31" >
        <activity
            android:name=".NavActivity"
            android:exported="true"
            android:label="@string/title_activity_nav"
            android:theme="@style/Theme.Otusfragmentlesson.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".finish.MainActivityFinish"
            android:exported="true" />
        <activity
            android:name=".start.MainActivity"
            android:exported="true" />
        <activity
            android:name=".lesson2.master_detail.finish.MasterDetailFinishActivity"
            android:exported="true" />
        <activity
            android:name=".lesson2.master_detail.start.MasterDetailActivity"
            android:exported="true" />
    </application>

</manifest>